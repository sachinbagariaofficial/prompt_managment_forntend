export type PropsTypes = {
    theme: {
        card: string;
        text: {
            primary: string;
            accent: string;
            muted: string;
            secondary: string
        };
        input: string
        badge: string
    };
    isDarkMode?: boolean;
};