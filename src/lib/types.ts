export type PropsTypes = {
    theme: {
        card: string;
        text: {
            primary: string;
            accent: string;
            muted: string;
        };
        input: string
    };
    isDarkMode?: boolean;
};